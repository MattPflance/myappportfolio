<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:gravity="center"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    tools:context=".MainActivity"
    tools:showIn="@layout/activity_main" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/default_margin"
        android:orientation="vertical"
        android:gravity="center">

        <Button android:id="@+id/popular_movies_button"
            style="@style/project_button"
            android:text="@string/popular_movies" />

        <Button android:id="@+id/library_app"
            style="@style/project_button"
            android:text="@string/alexandria" />

        <Button android:id="@+id/scores_app_button"
            style="@style/project_button"
            android:text="@string/football_scores" />

        <Button android:id="@+id/build_it_bigger_button_free"
            style="@style/project_button"
            android:text="@string/build_free" />

        <Button android:id="@+id/build_it_bigger_button_paid"
            style="@style/project_button"
            android:text="@string/build_paid" />

        <Button android:id="@+id/xyz_reader_button"
            style="@style/project_button"
            android:text="@string/xyz" />

        <Button android:id="@+id/capstone_button"
            style="@style/project_button"
            android:background="@color/violet"
            android:text="@string/capstone" />

    </LinearLayout>

</ScrollView>
